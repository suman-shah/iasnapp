<!-- res/drawable/rounded_corner_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color -->
    <corners android:radius="20dp"/> <!-- Adjust the radius as needed -->
    <padding android:paddingTop="8dp" android:paddingRight="8dp" android:paddingBottom="16dp" android:paddingLeft="16dp"/>
</shape>
